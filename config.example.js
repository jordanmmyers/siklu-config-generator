var config = {
    password    :   "set user admin passw password",
    snmp        :   {
        "set snmp-agent read-com public write-com private snmp-version v2c",
        "set snmp-mng 1 ip-addr 10.0.0.1 security-name public"
    },
};
